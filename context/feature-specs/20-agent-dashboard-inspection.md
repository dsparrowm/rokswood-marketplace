Read `AGENTS.md` before starting

Read `context/progress-tracker.md`

read the backend(rokswood-hive-backend-api) for context

check the agent dashboard ui and compare what is available on the backend on /agents/ endpoints to see what we can convert from dummy data to backend-backed data

list what is implemented on the backend and what is not