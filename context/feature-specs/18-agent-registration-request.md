Read `AGENTS.md` before starting

Read `context/progress-tracker.md`

read the backend(rokswood-hive-backend-api) for context

implement the agent registration flow end to end by making sure the form is connected to the backend endpoint at /public/agent-requests(search the backend for schema structure to make sure the frontend is sending the right data)



success criteria:
Agent can register using the agent registration form
admin
sys-admin on rokswood-hive-web can approve the agent request